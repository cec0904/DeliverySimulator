using UnityEngine;

public class Quest : MonoBehaviour
{
    // 이 스크립트를 가지고 있다면 퀘스트를 주고 받을 수 있음




    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
